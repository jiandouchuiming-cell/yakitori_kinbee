# プロジェクトルール

- 会話およびコード内のコメントはすべて日本語で記述すること。
- `git add`, `git commit`, `git push` などのGit操作は自動で行わず、必ずユーザーに確認を取り、指示があった場合にのみ実行すること。その際、変更点を簡潔にまとめ、コミットメッセージの文言（案）を提案すること。
- ファイル変更などの操作を行う前には、元の状態に戻せるようにバックアップ（変更前ファイルのコピーなど）を作成すること。ただし、`git push` を実行した後は、それ以前のバックアップは不要とする（`git push` 前の状態に戻すことはないため）。
